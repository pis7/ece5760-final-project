Introduction
-------------------------------------------------------------------------------
We accelerate analytical global placement based on a qudratic wirelength
modle by using the FPGA to run congugate gradient decent in parallel. This
works by parallelizing the vector vector and vector scalar multiplications
(we cannot paralellize the matrix vector multiplications because the loop
used to do multiplication for the sparse representation we use, CSR, has
cross iteration data dependencies.*)

The problem we solve is as follows: Given a set of "macros," pieces of
hardware, connected by wires, we place the macros on a plane such that the
total wirelength to connect all macros is as small as possible. This is the
first step of place and route for ASICs.

```bash
uv run run-placer arm benchmarks/custom/parallel_chains_50
uv run run-placer fpga benchmarks/custom/parallel_chains_50
````

Compare cg-total, cg-average, and placer total for all of these

```bash
uv run run-placer verilated v6 benchmarks/custom/parallel_chains_50 --sweep
uv run vis parallel_chains_50.json
```
We can see what the placer is doing at each step using the above command.

These all started from lef/def files. The lef file is a library of macros
and the def files is an inital placement which we optimize.

```
vim benchmarks/custom/parallel_chains_50/def/parallel_chains_50.lef
vim benchmarks/custom/parallel_chains_50/def/parallel_chains_50.def
```

This is cool, but it's hard to get a feel for the actual design. Let's start
from verilog.

```
vim ../project1-group11/rtl/Demo2.v
```
Write some verilog code!!!
This uses infrastructure and a project Parker and Jeremy worked on for the
ASICs class to parse and lower the Verilog to lef/def.

```
cd ../project1-group11/asic/build-demo2/
./run-flow
cd -
cp -r ../project1-group11/asic/build-demo2/03-summarize-results/ benchmarks/custom

uv run run-placer fpga benchmarks/custom/03-summarize-results/
uv run run-placer verilated v6 benchmarks/custom/03-summarize-results/ --sweep

uv run vis 03-summarize-results.json
```

That's it.

* Without extreme cleverness.
